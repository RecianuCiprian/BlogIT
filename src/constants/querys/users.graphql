query userInfo($id : JSON!) {
    users(filters:$id) {
        _id
        username
        firstname
        lastname
        email
        groupIds
    }
}

query allUsers {
    users {
        _id
        username
        firstname
        lastname
        email
        groupIds
    }
}